#include<stdio.h>
int main()
{
	int n,i;
	printf("输入一个大于三的整数:\n");
	scanf("%d",&n);
	for(i=2;i<n;i++)
		if(n%i==0)
			break;
		if(i<n)
			printf("%d不为素数\n",n);
		else
			printf("%d为素数\n",n);
}
//判断一个数是否为素数
